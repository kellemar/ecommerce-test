export enum UserRole {
  Admin = 'admin',
  Customer = 'customer',
}

export enum ProductStatus {
  Draft = 'draft',
  Published = 'published',
  Archived = 'archived',
}

export enum OrderStatus {
  Pending = 'pending',
  Paid = 'paid',
  Fulfilled = 'fulfilled',
  Cancelled = 'cancelled',
}
